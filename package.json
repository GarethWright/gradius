{
  "name": "gradius-star-command",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "build": "vite build",
    "check": "node --check src/game.js && node --check src/scene.js",
    "deploy": "npm run build && wrangler deploy",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node tests/game.test.cjs && node tests/scene.test.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "wrangler": "^4.0.0"
  },
  "type": "module",
  "dependencies": {
    "three": "^0.186.0"
  }
}
